-- ---------------------------------------------------------------------------
-- Редактируемые тексты праздничных баннеров.
--
-- Раньше заголовок и подпись были впечатаны прямо в картинку баннера, поэтому
-- поменять их можно было только перерисовав файл. Теперь текст выводится
-- HTML-слоем поверх изображения и берется отсюда — администратор правит его на
-- вкладке «Редактор текстов».
--
-- Заголовок и подпись — отдельные ключи, а не одна строка «Заголовок: подпись».
-- Прежний формат приходилось делить по первому двоеточию, и двоеточие внутри
-- самого заголовка ломало разбор.
-- ---------------------------------------------------------------------------

INSERT INTO cms_content (key, value) VALUES
  ('themeBannerNewYearTitle',     'Новый год в Colvir'),
  ('themeBannerNewYearSubtitle',  'Зимняя подборка мероприятий внутри'),
  ('themeBannerSpringTitle',      'Весна в Colvir'),
  ('themeBannerSpringSubtitle',   'Весенняя подборка мероприятий внутри'),
  ('themeBannerBirthdayTitle',    'День рождения компании'),
  ('themeBannerBirthdaySubtitle', 'Праздничная подборка мероприятий внутри')
ON CONFLICT (key) DO NOTHING;

-- Старые склеенные строки больше не используются интерфейсом. Не удаляем их:
-- администратор мог что-то там поправить, и эти значения пригодятся, если
-- понадобится перенести формулировки вручную.
